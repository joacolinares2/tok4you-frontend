import React from "react";
import RegisterComponent from "@/components/registro/comRegistro";
import Layout from "@/components/LayoutBars";

const RegisterPage: React.FC = () => {
  return (
    <Layout>
      <RegisterComponent />
    </Layout>
  );
};

export default RegisterPage;
